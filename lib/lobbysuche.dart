import 'package:flutter/material.dart';

class LobbySuche extends StatelessWidget {
  final String winnerName;

  LobbySuche({required this.winnerName});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
          ],
        ),
      ),
    );
  }
}